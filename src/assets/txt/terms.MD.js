import {appendLine, appendText} from "../../util/utils";

let text = "";
text = appendLine(text,"# Terms and Agreement");
text = appendLine(text,"This is the official terms of use for play now website");

export default text;
